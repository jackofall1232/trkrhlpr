# Run Robolectric unit tests on a well-supported emulated SDK. The Room migration SQL under
# test is SQLite-level and SDK-independent, so this avoids needing Robolectric support for the
# project's compileSdk (36) while still exercising the real Room stack.
sdk=33
